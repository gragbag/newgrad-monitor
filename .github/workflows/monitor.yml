name: New Grad Job Monitor

on:
  schedule:
    # Runs every 2 hours. Adjust the cron as you like (this is UTC).
    - cron: "0 */2 * * *"
  workflow_dispatch: {}  # lets you trigger it manually from the Actions tab

permissions:
  contents: write  # needed so the workflow can commit the updated jobs.json

jobs:
  check-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install requests

      - name: Run monitor
        env:
          USAJOBS_API_KEY: ${{ secrets.USAJOBS_API_KEY }}
          USAJOBS_EMAIL: ${{ secrets.USAJOBS_EMAIL }}
        run: python monitor.py

      - name: Commit updated jobs.json
        run: |
          git config user.name "newgrad-monitor-bot"
          git config user.email "actions@users.noreply.github.com"
          git add jobs.json
          git diff --quiet --cached || git commit -m "Update jobs.json [skip ci]"
          git push
